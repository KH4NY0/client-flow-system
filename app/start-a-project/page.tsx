'use client';import {useState} from 'react';import Nav from '../components/Nav';
const options=['Website or landing page','Brand identity or rebrand','Design and product support','Automation or internal tool','Something else'];
export default function StartProject(){const [step,setStep]=useState(1),[type,setType]=useState(''),[done,setDone]=useState(false);const next=()=>setStep(s=>Math.min(4,s+1)); if(done)return <><Nav/><main className="form-wrap"><div className="eyebrow"><i className="dot"/> Project brief received</div><div className="form-card success"><span className="tag">Thank you</span><h2>Your project brief is in.</h2><p>You will receive a considered response within two working days. If there is a strong fit, the next step is a focused discovery conversation.</p></div></main></>;return <><Nav/><main className="form-wrap"><div className="eyebrow"><i className="dot"/> Start a project</div><h1>Tell me what you are trying to make better.</h1><p>A few thoughtful details will help me prepare before we speak.</p><div className="progress">{[1,2,3,4].map(n=><i key={n} className={n<=step?'active':''}/>)}</div><div className="form-card">{step===1&&<><h2>What are you looking to make or improve?</h2><div className="choices">{options.map(o=><button className={'choice '+(type===o?'selected':'')} onClick={()=>setType(o)} key={o}>{o}</button>)}</div></>}{step===2&&<><h2>Tell me a little about the context.</h2><div className="field"><label>Company or project name</label><input placeholder="Your company"/></div><div className="field"><label>Website, if applicable</label><input placeholder="https://"/></div><div className="field"><label>What needs to change or become possible?</label><textarea rows={4} placeholder="A useful starting point is enough."/></div></>}{step===3&&<><h2>What would a successful outcome look like?</h2><div className="field"><label>Your ideal launch window</label><select defaultValue=""><option value="" disabled>Select a timing</option><option>Within 4 weeks</option><option>1 to 3 months</option><option>3 to 6 months</option><option>Still exploring</option></select></div><div className="field"><label>Indicative investment range, optional</label><select defaultValue=""><option value="" disabled>Select a range</option><option>Under R25,000</option><option>R25,000 to R50,000</option><option>R50,000 to R100,000</option><option>R100,000+</option></select></div><div className="field"><label>Who is involved in the decision?</label><input placeholder="Names or roles are useful"/></div></>}{step===4&&<><h2>Where should I send the next step?</h2><div className="field"><label>Your name</label><input placeholder="Name"/></div><div className="field"><label>Work email</label><input type="email" placeholder="name@company.com"/></div><div className="field"><label>Anything else that will help me prepare?</label><textarea rows={3} placeholder="Optional"/></div></>}<div className="form-actions"><button className="pill light" onClick={()=>setStep(s=>Math.max(1,s-1))} style={{visibility:step===1?'hidden':'visible'}}>Back</button>{step<4?<button className="pill blue" disabled={step===1&&!type} onClick={next}>Continue</button>:<button className="pill blue" onClick={()=>setDone(true)}>Send project brief</button>}</div></div></main></>}
